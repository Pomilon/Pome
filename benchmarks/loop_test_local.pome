import time;

// N is injected by runner
fun run() {
    var i = 0;
    while (i < N) {
        i = i + 1;
    }
}

var start = time.clock();
run();
var end = time.clock();
print("Time: " + (end - start) + "s");
