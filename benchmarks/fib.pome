import time;

// Recursive Fibonacci
fun fib(n) {
    if (n < 2) return n;
    return fib(n - 1) + fib(n - 2);
}

// N is injected by the benchmark runner
print("Calculating Fib(" + N + ")...");
var start = time.clock();
var res = fib(N);
var elapsed = time.clock() - start;

print("Fib(" + N + ") = " + res);
print("Time: " + elapsed + "s");
