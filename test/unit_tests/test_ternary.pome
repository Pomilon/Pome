var a = 10;
var b = 20;

// Basic ternary
var max = a > b ? a : b;
print("Max of", a, "and", b, "is", max); // Expect 20

var nested = a > b ? "A" : b > 15 ? "B" : "C";
print("Nested:", nested);

// Truthiness
var result = "string" ? "truthy" : "falsy";
print("String is", result); // Expect truthy
var result2 = 0 ? "truthy" : "falsy"; // 0 is truthy in Pome?
print("0 is", result2);
